
import Layout from '../../components/Layout';
import BookingProgress from '../../components/customer/BookingProgress';

const BookingProgressPage = () => {
  return (
    <Layout>
      <BookingProgress />
    </Layout>
  );
};

export default BookingProgressPage;
